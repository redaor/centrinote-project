/// <reference types="vite/client" />

interface ImportMetaEnv {
  // Note: Les clés OpenAI sont maintenant stockées uniquement dans Supabase Edge Functions
  // Aucune variable VITE_OPENAI_* n'est plus utilisée côté client
}
